ARI Next Intent-First V1 changes the primary ARI Next interaction from a large structured filter card to a plain-language intent prompt with optional collapsed refinements. It removes user-facing Action Network loading terminology, keeps Circle location/radius centralized in Connect, surfaces current unranked options when no intent exists, and routes the production Circle context request through a public Supabase configuration bridge while retaining the user JWT as authorization authority.
