# Domain Events V1 stack position

`main -> #81 Action Network -> #82 Mission V2 -> #83 Places/Explore -> #84 trusted Mission actions -> #85 Match V2 -> Domain Events V1`

Domain Events V1 must target Match V2 and must not be retargeted directly to `main` until the dependency stack is collapsed and validated in order.
